# USER AUTHENTICATION API AND MONGOOSE TUTORIAL PROJECT

<!-- - Update api for - products, order and user information -->

# AIM

- Learn user authentication and jwt usage
- create protected routes for user based on roles
- learn mongoose and mongoDb storage handling

# RUN LOCALLY

- fill your .env file with the appropriate values for these variables
  - MONGO_URI=
  - PORT=5000
  - PAYSTACK_SECRET_KEY=
  - PAYSTACK_PAYMENT_API=
  - PAYSTACK_VERIFICATION_API=
  - SALT_LENGTH=
  - JWT_SECRET=

```
<!-- git clone the repo or download the package directly from youtube -->
<!-- Go to the root dir and run -->
pnpm i
<!-- start the server with -->
pnpm run dev
```

If your .env file is configured right, your server should be running now
